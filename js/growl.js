
( function( mdash, $ )
{
    
    // WIP
    
    var Growl = mdash.Growl = {
        
        show : function( message, options )
        {
            // noop;
        }
    };
    
} )( window.mdash, window.jQuery || window.Zepto );
